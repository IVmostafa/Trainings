import { RoundNotePipe } from './round-note.pipe';

describe('RoundNotePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundNotePipe();
    expect(pipe).toBeTruthy();
  });
});
