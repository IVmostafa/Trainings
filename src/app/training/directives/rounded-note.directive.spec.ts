import { RoundedNoteDirective } from './rounded-note.directive';

describe('RoundedNoteDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundedNoteDirective();
    expect(directive).toBeTruthy();
  });
});
